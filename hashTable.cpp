#include "../include/interface.h"
#include <iostream>

uint32_t hash2(uint32_t key)
{
    // todo
}

void hashTable::insert(Histogram hist, uint32_t index)
{
    // we need histogram's index
    // which matches a specific (1) partition

    // for loop
    // put all tuples in this specific partition
    // in the correct place of the hashTable

}